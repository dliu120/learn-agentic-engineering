---
moduleId: foundations-prompts-to-harnesses
lessonId: prompt-context-harness
title: "The Engineering Scope Ladder"
order: 1
topicNumbers: [1]
summary: "Learn the five scopes of LLM engineering and why each new stage exists."
estimatedMinutes: 10
prerequisites: []
references:
  - title: "Building Effective Agents"
    url: "https://www.anthropic.com/engineering/building-effective-agents"
    source: "Anthropic Engineering"
  - title: "Agents Guide"
    url: "https://developers.openai.com/api/docs/guides/agents"
    source: "OpenAI Developers"
deepDiveTopics:
  - "Workflow vs. agent"
  - "Deterministic vs. model-directed control"
objectives:
  - "Name the five engineering stages from prompt to graph"
  - "Explain what control each stage adds"
  - "Choose the simplest stage that can solve a task"
hasAudio: true
narration: >
  LLM systems become easier to reason about when you stop calling every design decision
  prompt engineering. There are five useful scopes. Prompt engineering designs one request.
  Conversation engineering manages what carries across turns. Response engineering gives the
  answer a typed contract. Loop engineering repeats model and tool steps with budgets and
  verification. Graph engineering makes branches, parallel work, state, and human gates
  explicit. Each stage solves a limitation in the one before it. The goal is not to reach the
  most complicated stage. The goal is to stop at the simplest stage that can meet the
  reliability requirement.
visualizations:
  - title: "Five stages of LLM engineering"
    summary: "A five-step progression from one prompt to conversation, response contracts, bounded loops, and explicit workflow graphs."
quiz:
  - id: q1
    type: ordering
    question: "Put the engineering stages in order from smallest to largest control scope."
    options:
      - "Prompt engineering"
      - "Conversation engineering"
      - "Response engineering"
      - "Loop engineering"
      - "Graph engineering"
    correct: [0, 1, 2, 3, 4]
    explanation: "Each stage adds a wider control surface: one request, multiple turns, a response contract, repeated execution, then explicit topology and state."
  - id: q2
    type: mcq
    question: "A task needs two independent research steps to run in parallel and join before review. What is the smallest fitting stage?"
    options:
      - "Graph engineering"
      - "Prompt engineering"
      - "Conversation engineering"
      - "Response engineering"
    correct: 0
    explanation: "Parallel branches and joins are graph structure. A larger prompt cannot make those dependencies explicit or observable."
  - id: q3
    type: mcq
    question: "Why should you avoid jumping straight to a multi-agent graph?"
    options:
      - "Complexity adds cost, coordination failures, and more state to debug"
      - "Graphs cannot call language models"
      - "Single prompts are always more accurate"
      - "Multi-agent systems cannot use tools"
    correct: 0
    explanation: "Use the simplest architecture that meets the requirement. Extra nodes and agents create new failure modes without guaranteeing better results."
isModuleGate: false
---

import VizWrapper from '@/components/viz/VizWrapper.astro';
import ConceptCard from '@/components/ui/ConceptCard.astro';
import KeyInsightCallout from '@/components/ui/KeyInsightCallout.astro';
import MisconceptionFlip from '@/components/ui/MisconceptionFlip.astro';
import DeepDive from '@/components/ui/DeepDive.astro';

<ConceptCard title="One model, five engineering scopes" tag="orientation">

- **Prompt engineering** shapes one request.
- **Conversation engineering** manages turns and context.
- **Response engineering** defines what a valid answer looks like.
- **Loop engineering** repeats model and tool work with budgets and checks.
- **Graph engineering** makes branches, joins, cycles, state, and human gates explicit.

</ConceptCard>

<KeyInsightCallout>
The stages are not maturity badges. Choose the **smallest control structure** that can meet the task's reliability needs.
</KeyInsightCallout>

<VizWrapper
  name="engineering-stages"
  title="Five stages of LLM engineering"
  summary="Prompt, Conversation, Response, Loop, and Graph engineering appear in order. Each stage answers a larger control question and keeps the earlier stages inside it."
  height={360}
/>

## A worked example: extract one invoice

Suppose you receive one invoice and need the vendor, total, currency, and due date.

1. Start with **prompt engineering**: write a clear extraction instruction and include one example.
2. If a user corrects the invoice over several messages, add **conversation engineering**.
3. If code must consume the result, add **response engineering** with a schema.
4. If missing fields require lookup tools and retries, add **loop engineering**.
5. If several documents can be checked in parallel and require an approval step, add **graph engineering**.

The task did not become “more agentic.” Its **control requirements expanded**.

<MisconceptionFlip
  claim={"Graph engineering replaces prompt engineering."}
  correction={"Every graph node still needs good prompts and response contracts. Larger stages contain the smaller ones."}
/>
<MisconceptionFlip
  claim={"More agents means more capability."}
  correction={"Extra agents help only when specialization or parallel work outweighs coordination cost."}
/>

<DeepDive title="Workflow vs. agent">

A **workflow** follows control flow defined mostly by code: run A, then B, then choose C or D.
An **agent** gives the model more authority to choose actions or the next step.

Both can use graphs. A graph is a way to make control flow explicit; it does not automatically
mean “multi-agent.”

</DeepDive>
